---
phase: 01-site-foundation-hub-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - games/where-winds-meet/cover.svg
  - games/where-winds-meet/index.html
  - games/where-winds-meet/tools/spawn-timer/index.html
autonomous: true
requirements: [HUB-02, HUB-03, HUB-04, THEME-01, THEME-02]
must_haves:
  truths:
    - "Navigating to /games/where-winds-meet/ shows the WWM game page with a breadcrumb and a Spawn Timer tool card"
    - "The Spawn Timer tool card links to /games/where-winds-meet/tools/spawn-timer/ (a real stub page, not a 404)"
    - "Every non-root page shows a breadcrumb trail with clickable ancestor segments and a non-link current segment"
    - "WWM pages render the jade accent because data-game=\"where-winds-meet\" is set on body"
    - "User can navigate hub → game → tool and back via breadcrumb on every page"
  artifacts:
    - path: "games/where-winds-meet/cover.svg"
      provides: "Drop-in styled SVG cover-art placeholder"
      contains: "viewBox"
    - path: "games/where-winds-meet/index.html"
      provides: "WWM game page: breadcrumb + tools registry + card renderer"
      min_lines: 40
      contains: "data-game=\"where-winds-meet\""
    - path: "games/where-winds-meet/tools/spawn-timer/index.html"
      provides: "Themed spawn-timer stub page (Phase 2 replaces body in place)"
      min_lines: 25
      contains: "aria-label=\"Breadcrumb\""
  key_links:
    - from: "games/where-winds-meet/index.html"
      to: "/games/where-winds-meet/tools/spawn-timer/"
      via: "tools registry href rendered into card"
      pattern: "/games/where-winds-meet/tools/spawn-timer/"
    - from: "games/where-winds-meet/index.html"
      to: "/"
      via: "breadcrumb ancestor link to hub root"
      pattern: "href=\"/\""
    - from: "games/where-winds-meet/tools/spawn-timer/index.html"
      to: "/games/where-winds-meet/"
      via: "breadcrumb ancestor link to game page"
      pattern: "href=\"/games/where-winds-meet/\""
    - from: "games/where-winds-meet/index.html"
      to: "/shared/theme.css"
      via: "root-absolute stylesheet link"
      pattern: "href=\"/shared/theme.css\""
---

<objective>
Complete the full hub → game → tool URL hierarchy on top of the plan-01 spine: the Where Winds Meet game page (with breadcrumb + tools registry rendering a Spawn Timer card), the drop-in cover-art SVG, and the themed Spawn Timer stub page at its real, bookmarkable URL. After this plan a user can click hub → WWM game page → Spawn Timer stub and back via breadcrumb on every level, with the jade accent applied — proving the whole hierarchy end-to-end with zero 404s.

Purpose: Realizes HUB-02 (game page lists tools), HUB-03 (consistent breadcrumb nav), HUB-04 (bookmarkable 3-level URLs, no 404 via the real stub per D-09), and applies the THEME-02 accent override on real pages. The stub page is the integration seam Phase 2 replaces in place — the URL and breadcrumb already exist.
Output: `games/where-winds-meet/cover.svg`, `games/where-winds-meet/index.html`, `games/where-winds-meet/tools/spawn-timer/index.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md
@.planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md
@.planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md
@.planning/phases/01-site-foundation-hub-shell/01-01-SUMMARY.md

<interfaces>
<!-- Consumed from plan 01 (do NOT redefine theme.css or the registry/renderer signature). -->

shared/theme.css (from plan 01) provides: --color-bg, --color-surface, --color-surface-raised,
--color-border, --color-text-primary, --color-text-secondary, --color-accent, --color-accent-secondary,
spacing tokens --space-*, typography tokens --text-*, .site-header/.site-title/.card-grid/.card/
.card__*/.site-footer component classes, global :focus-visible ring, and the
[data-game="where-winds-meet"] accent override block.

Registry/renderer contract (CF-04) established in plan 01 index.html — reuse the same shape here for tools:
  const tools = [{ name, description, href }, ...];
  renderCards-style template-literal function → container.innerHTML on DOMContentLoaded.

Breadcrumb contract (D-07/D-08, RESEARCH Pattern 3) introduced in THIS plan and reused on the stub:
  const breadcrumbItems = [{ label, href }, ..., { label, href: null }];  // null = current page
  inline renderer builds <ol> with <a> for ancestors and <span aria-current="page"> for the leaf,
  injected into <nav aria-label="Breadcrumb" id="breadcrumb">.
  Separators are CSS-generated (li + li::before), NOT in markup (RESEARCH Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the WWM game page with breadcrumb and tools registry</name>
  <files>games/where-winds-meet/cover.svg, games/where-winds-meet/index.html</files>
  <read_first>
    - shared/theme.css (tokens + component classes from plan 01 — link and reuse, do not redefine)
    - .planning/phases/01-site-foundation-hub-shell/01-01-SUMMARY.md (what plan 01 created; the exact registry/renderer shape used on the hub)
    - .planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md (Game Page layout, Breadcrumb Strip, Cover Art Placeholder SVG, Copywriting Contract, Interaction States)
    - .planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md (CF-02, CF-04, D-03, D-05, D-06, D-07, D-08)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pattern 1 registry; Pattern 3 JS-injected breadcrumb; Accessible Breadcrumb CSS; Game Page Shell example)
  </read_first>
  <action>
    Create `games/where-winds-meet/cover.svg` per UI-SPEC Cover Art Placeholder: 280×160 viewBox, vertical gradient from #1a1d23 (top) to #0f3d2d (jade-tinted bottom), centered title text "Where Winds Meet" in #e8e4d8 at ~14px weight 600, a jade horizontal rule (2px, ~40px wide, #3fb98f) above the title, and a small gold (#d4af37) decorative accent dot below. This is a drop-in file (D-06) swappable for real art with no code change; the hub card already references it root-absolute.

    Create `games/where-winds-meet/index.html` (CF-02 fixed path). Head: charset, viewport, `<title>Where Winds Meet — Game Tools Hub</title>`, favicon link `/favicon.svg`, stylesheet `/shared/theme.css` (root-absolute). Set `data-game="where-winds-meet"` on `<body>` (D-03 — this triggers the jade accent override from theme.css; THEME-02). Layout per UI-SPEC: `<header class="site-header">` with the "Game Tools Hub" site title (same as hub), then `<nav aria-label="Breadcrumb" id="breadcrumb">` (empty container, populated by inline JS), then `<main>` with `<h1>Where Winds Meet</h1>`, an intro line "Tools for Where Winds Meet" (Label size, secondary color), and `<div id="card-grid" class="card-grid"></div>`, then `<footer class="site-footer">Game Tools Hub — personal project</footer>`.

    Add inline JS with two pieces. (1) breadcrumbItems = [{ label: "Game Tools Hub", href: "/" }, { label: "Where Winds Meet", href: null }] and a renderer that builds an `<ol>` where ancestor items become `<a href>` links and the final item (href null) becomes `<span aria-current="page">`, injected into the #breadcrumb nav (D-07/D-08, RESEARCH Pattern 3). Do NOT put separator characters in the markup — separators come from CSS `li + li::before` defined in theme.css/page CSS (RESEARCH Pitfall 4). (2) tools registry = [{ name: "Spawn Timer", description: "GvG jungle spawn timer", href: "/games/where-winds-meet/tools/spawn-timer/" }] and a card renderer (same template-literal pattern as the hub, CF-04) that builds an `<a class="card">` wrapping a `.card__body` with the tool name (Heading) and description (Label, secondary). No cover image on tool cards per UI-SPEC. Render on DOMContentLoaded into "card-grid" (HUB-02). Keep the tools array drop-in extensible (HUB-05). Do NOT pass URL-derived or user-supplied strings into innerHTML (static registry only).
  </action>
  <verify>
    <automated>cd "E:/Projekty/GithubIO" && grep -q "viewBox" games/where-winds-meet/cover.svg && grep -q "3fb98f" games/where-winds-meet/cover.svg && grep -q "data-game=\"where-winds-meet\"" games/where-winds-meet/index.html && grep -q "aria-label=\"Breadcrumb\"" games/where-winds-meet/index.html && grep -q "aria-current=\"page\"" games/where-winds-meet/index.html && grep -q "href=\"/\"" games/where-winds-meet/index.html && grep -q "/games/where-winds-meet/tools/spawn-timer/" games/where-winds-meet/index.html && grep -q "href=\"/shared/theme.css\"" games/where-winds-meet/index.html && test -z "$(git ls-files games/where-winds-meet/index.html games/where-winds-meet/cover.svg | grep -E '[A-Z]')" && echo PASS</automated>
    <human-check>Serve repo root, open http://localhost:8080/games/where-winds-meet/: breadcrumb reads "Game Tools Hub / Where Winds Meet" (first segment is a working link to /, last segment is jade and not a link), a Spawn Timer tool card is visible and links to the stub, and the page background is dark with jade accent on the card top border.</human-check>
  </verify>
  <acceptance_criteria>
    - `games/where-winds-meet/cover.svg` has a `viewBox` and contains jade `#3fb98f`
    - `games/where-winds-meet/index.html` body carries `data-game="where-winds-meet"`
    - The page links `/shared/theme.css` and `/favicon.svg` root-absolutely
    - `<nav aria-label="Breadcrumb" id="breadcrumb">` exists; breadcrumbItems first entry has `href: "/"` and last entry has `href: null`
    - The breadcrumb renderer emits `aria-current="page"` on the leaf segment and `<a>` on ancestors
    - tools registry entry href is `/games/where-winds-meet/tools/spawn-timer/`
    - No separator characters (e.g. "/") appear inside the breadcrumb markup string (they are CSS-generated)
    - No uppercase filenames introduced
  </acceptance_criteria>
  <done>WWM game page renders the breadcrumb (clickable hub link + non-link current segment) and a Spawn Timer tool card linking to the stub URL, with the jade accent applied via data-game.</done>
</task>

<task type="auto">
  <name>Task 2: Build the themed Spawn Timer stub page (Phase 2 seam, no 404)</name>
  <files>games/where-winds-meet/tools/spawn-timer/index.html</files>
  <read_first>
    - games/where-winds-meet/index.html (the breadcrumb renderer + theme links established in Task 1 — reuse the same breadcrumb contract and head structure)
    - shared/theme.css (tokens + classes from plan 01)
    - .planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md (Spawn-Timer Stub Page layout, Copywriting Contract stub copy, Breadcrumb Strip)
    - .planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md (CF-02, D-07, D-09)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pattern 3 breadcrumb; D-09 stub seam rationale)
  </read_first>
  <action>
    Create `games/where-winds-meet/tools/spawn-timer/index.html` (CF-02 fixed path; this is the real, bookmarkable tool URL so there is no 404 — D-09, HUB-04). Head: charset, viewport, `<title>Spawn Timer — Where Winds Meet — Game Tools Hub</title>`, favicon link `/favicon.svg`, stylesheet `/shared/theme.css` (root-absolute). Set `data-game="where-winds-meet"` on `<body>` so the jade accent applies (THEME-02). Layout per UI-SPEC: `<header class="site-header">` with the "Game Tools Hub" title, then `<nav aria-label="Breadcrumb" id="breadcrumb">`, then a `<main>` stub content area (vertically centered) containing `<h1>Spawn Timer</h1>`, a subheading "GvG jungle spawn timer" (Label/secondary), and body copy "Coming in Phase 2. This page is your bookmark — the timer will load here when it's ready." Then `<footer class="site-footer">Game Tools Hub — personal project</footer>`. NO interactive timer controls (that is Phase 2; this body is the seam Phase 2 replaces in place).

    Add inline JS for the breadcrumb only, reusing the SAME breadcrumb renderer pattern as the game page: breadcrumbItems = [{ label: "Game Tools Hub", href: "/" }, { label: "Where Winds Meet", href: "/games/where-winds-meet/" }, { label: "Spawn Timer", href: null }] injected into #breadcrumb. Ancestors are `<a>` links; the leaf "Spawn Timer" is `<span aria-current="page">`. Separators remain CSS-generated, not in markup.
  </action>
  <verify>
    <automated>cd "E:/Projekty/GithubIO" && grep -q "data-game=\"where-winds-meet\"" games/where-winds-meet/tools/spawn-timer/index.html && grep -q "aria-label=\"Breadcrumb\"" games/where-winds-meet/tools/spawn-timer/index.html && grep -q "href=\"/games/where-winds-meet/\"" games/where-winds-meet/tools/spawn-timer/index.html && grep -q "href=\"/\"" games/where-winds-meet/tools/spawn-timer/index.html && grep -q "Coming in Phase 2" games/where-winds-meet/tools/spawn-timer/index.html && grep -q "href=\"/shared/theme.css\"" games/where-winds-meet/tools/spawn-timer/index.html && test -z "$(git ls-files games/where-winds-meet/tools/spawn-timer/index.html | grep -E '[A-Z]')" && echo PASS</automated>
    <human-check>Serve repo root, open http://localhost:8080/games/where-winds-meet/tools/spawn-timer/: breadcrumb reads "Game Tools Hub / Where Winds Meet / Spawn Timer" with the first two segments clickable (to / and /games/where-winds-meet/) and "Spawn Timer" as the jade current segment; the stub shows "Coming in Phase 2…" copy and no timer controls; dark theme + jade accent applied.</human-check>
  </verify>
  <acceptance_criteria>
    - `games/where-winds-meet/tools/spawn-timer/index.html` exists at the exact CF-02 path (real URL, no 404)
    - Body carries `data-game="where-winds-meet"`; links `/shared/theme.css` and `/favicon.svg` root-absolutely
    - Breadcrumb has three items; ancestors link to `/` and `/games/where-winds-meet/`; leaf "Spawn Timer" is `aria-current="page"` and not a link
    - Stub body contains the copy "Coming in Phase 2"
    - No interactive timer controls present (no inputs/buttons for timer behavior)
    - No uppercase filenames introduced
  </acceptance_criteria>
  <done>The full 3-level hierarchy is reachable: hub → game → spawn-timer stub, each with a correct breadcrumb and back-navigation, jade-themed, with the stub URL bookmarkable and no 404 anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — inbound) | No user input, no forms, no query params; breadcrumb and tools data are static developer-defined literals. |
| browser → GitHub Pages CDN | Read-only static file fetch; no compute or state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering (XSS) | breadcrumb + tools renderers (innerHTML) | mitigate | breadcrumbItems and tools arrays are static developer-defined literals; never pass location.href, URL params, or document.cookie into innerHTML. Same safe-innerHTML convention as plan 01. |
| T-01-05 | Information Disclosure | public repo contents | accept | No secrets/PII; static personal hub. Nothing sensitive to disclose. |
| T-01-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH Package Legitimacy Audit: none). |
| — | Spoofing / Repudiation / DoS / Elevation | — | N/A | No auth, sessions, privileged actions, or server compute on a static no-input page. |
</threat_model>

<verification>
- cover.svg, game page, and stub page all exist at their CF-02 fixed paths; no 404 on any of the 3 levels.
- Both WWM pages set data-game="where-winds-meet" (jade accent applies; THEME-02).
- Breadcrumb present on both non-root pages with correct clickable ancestors and a non-link aria-current leaf (HUB-03).
- Game page renders the Spawn Timer tool card from the tools registry, linking to the stub (HUB-02).
- All asset/links root-absolute; `git ls-files | grep -E '[A-Z]'` over this plan's files returns zero.
</verification>

<success_criteria>
- User can navigate hub → WWM game page → Spawn Timer stub and back via breadcrumb on every page (HUB-03, HUB-04).
- The Spawn Timer tool card on the game page links to a real stub page, not a 404 (HUB-02, D-09).
- WWM pages render with the jade accent over the dark base (THEME-01, THEME-02).
- All three hierarchy URLs are bookmarkable and reachable under a local server (HUB-04; live verification in plan 03).
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-hub-shell/01-02-SUMMARY.md` when done.
</output>
