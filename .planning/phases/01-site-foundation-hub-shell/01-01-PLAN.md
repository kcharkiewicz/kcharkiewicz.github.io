---
phase: 01-site-foundation-hub-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nojekyll
  - shared/theme.css
  - favicon.svg
  - index.html
  - README.md
autonomous: true
requirements: [SITE-02, HUB-01, HUB-05, THEME-01, THEME-02, THEME-03]
must_haves:
  truths:
    - "Visiting / (served via local HTTP server) renders a dark-themed hub landing page"
    - "The hub shows exactly one game card for Where Winds Meet linking to /games/where-winds-meet/"
    - "The browser tab shows the custom GT SVG favicon"
    - "Adding a second object to the games registry array renders a second card with no other file edits"
    - "All tracked filenames are lowercase and .nojekyll exists at repo root"
  artifacts:
    - path: ".nojekyll"
      provides: "Jekyll bypass guard (empty file)"
      contains: ""
    - path: "shared/theme.css"
      provides: "Base dark-theme tokens + per-game accent override mechanism"
      contains: "--color-accent"
    - path: "favicon.svg"
      provides: "Custom GT-monogram SVG favicon"
      contains: "viewBox"
    - path: "index.html"
      provides: "Hub landing page with inline games registry + card renderer"
      min_lines: 40
      contains: "card-grid"
    - path: "README.md"
      provides: "Local dev-server run command + lowercase/case-audit note"
      contains: "http.server"
  key_links:
    - from: "index.html"
      to: "/shared/theme.css"
      via: "root-absolute stylesheet link"
      pattern: "href=\"/shared/theme.css\""
    - from: "index.html"
      to: "/games/where-winds-meet/"
      via: "games registry href rendered into card"
      pattern: "/games/where-winds-meet/"
    - from: "index.html"
      to: "/favicon.svg"
      via: "root-absolute icon link"
      pattern: "href=\"/favicon.svg\""
---

<objective>
Lay the walking-skeleton spine of the Game Tools Hub: the Jekyll-bypass guard, the shared dark-theme token system with a per-game accent mechanism, the custom favicon, and a live hub landing page that renders the Where Winds Meet card from an inline JS registry. After this plan, opening the served repo root shows a real, dark-themed, single-card hub — the thinnest end-to-end slice of the site.

Purpose: Establish the deployment guards (CF-05), the theme contract (CF-03/D-03), and the drop-in registry pattern (CF-04/HUB-05) that every later page and phase builds on. theme.css and the registry pattern are foundational contracts — they are defined here once and consumed (link-only, never re-edited) by all subsequent pages.
Output: `.nojekyll`, `shared/theme.css`, `favicon.svg`, `index.html`, `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md
@.planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md
@.planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md
@.planning/phases/01-site-foundation-hub-shell/01-SKELETON.md

<interfaces>
<!-- Contracts this plan ESTABLISHES for later plans/phases. Defined once here; downstream pages link/consume, never redefine. -->

shared/theme.css exposes these CSS custom properties on :root (base) and overrides them under [data-game="where-winds-meet"]:
  --color-bg, --color-surface, --color-surface-raised, --color-border,
  --color-text-primary, --color-text-secondary,
  --color-accent, --color-accent-secondary,
  --space-xs..--space-3xl, --text-body/--text-label/--text-heading/--text-display

Inline registry + renderer contract (CF-04), reused on the game page (plan 02):
  const games = [{ slug, name, toolCount, href, cover }, ...];
  function renderCards(items, containerId): builds <a class="card"> markup via template literals, sets container.innerHTML.
  Called on DOMContentLoaded against id="card-grid".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit deployment guards and shared theme tokens</name>
  <files>.nojekyll, shared/theme.css, README.md</files>
  <read_first>
    - .planning/STATE.md (Key Decisions Logged: .nojekyll first commit, all-lowercase, root-absolute paths; Pitfall Watch)
    - .planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md (CF-03, CF-05, D-01, D-02, D-03)
    - .planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md (Color Contract base tokens, Spacing Scale, Typography, Per-Game Accent Mechanism, focus-visible ring)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pattern 2 CSS custom-property theming; Pitfall 1 Jekyll; Pitfall 3 root-absolute paths; Validation Architecture local server)
  </read_first>
  <action>
    Create an EMPTY file `.nojekyll` at the repo root (E:/Projekty/GithubIO/.nojekyll) — this is the Jekyll-bypass guard and must exist before any underscore-sensitive asset structure (CF-05, STATE.md). It contains nothing.

    Create `shared/theme.css` defining the base token set on :root using the locked/spec values: --color-bg #0f1115, --color-surface #1a1d23, --color-surface-raised #22262f, --color-border #2a2e38, --color-text-primary #e8e4d8, --color-text-secondary #9a9690, --color-accent #3fb98f, --color-accent-secondary #d4af37. Add the spacing scale (--space-xs 4px, --space-sm 8px, --space-md 16px, --space-lg 24px, --space-xl 32px, --space-2xl 48px, --space-3xl 64px) and typography tokens (--text-body 16px/1.5, --text-label 14px/1.4, --text-heading 20px/1.2, --text-display 28px/1.1) with the system font stack "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif". Add the per-game accent override block `[data-game="where-winds-meet"]` re-asserting --color-accent #3fb98f (jade, per D-02) and --color-accent-secondary #d4af37 (gold, per D-02) so the override mechanism is provably reusable. Include a global reset/base (box-sizing border-box, body background var(--color-bg), color var(--color-text-primary), the system font, font-feature-settings "kern" 1) and a global :focus-visible rule: outline 2px solid var(--color-accent), outline-offset 2px. Add the shared component styles the hub and later pages reuse: .site-header (surface band, --space-md vertical padding), .site-title (Display size), .card-grid (grid, repeat(auto-fill, minmax(280px, 1fr)), justify-content center, gap var(--space-lg), max-width 960px, margin-inline auto, padding-inline var(--space-xl)), .card / .card__cover (160px height, surface-raised, object-fit contain) / .card__body / .card__name / .card__count, card 3px solid var(--color-accent) top border + 1px var(--color-border) sides + 8px radius + 150ms ease transition + hover to surface-raised with full accent border + min-height 220px, and .site-footer (Label size, secondary color, centered). Use auto-fill (NOT auto-fit) per RESEARCH Pitfall 5. All paths everything references must be root-absolute.

    Create `README.md` documenting: the local dev-server command `python -m http.server 8080` run from the repo root then open http://localhost:8080 (root-absolute paths only resolve under a server root, never file://), and a one-line case-audit note that `git ls-files | grep -E '[A-Z]'` must return zero results (all filenames lowercase, per CF-05). Keep it short — this is a dev note, not marketing.

    Do NOT create any uppercase or mixed-case filenames. Do NOT set `git config core.ignorecase false` on this Windows machine (RESEARCH Pitfall 2 — it breaks git on NTFS); enforce lowercase by convention only.
  </action>
  <verify>
    <automated>cd "E:/Projekty/GithubIO" && test -f .nojekyll && test ! -s .nojekyll && grep -q -- "--color-accent" shared/theme.css && grep -q "data-game=\"where-winds-meet\"" shared/theme.css && grep -q "auto-fill" shared/theme.css && grep -q "http.server" README.md && test -z "$(git ls-files shared/ README.md .nojekyll | grep -E '[A-Z]')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.nojekyll` exists at repo root and is empty (zero bytes)
    - `shared/theme.css` defines `--color-bg` as `#0f1115` and `--color-accent` as `#3fb98f` on :root
    - `shared/theme.css` contains a `[data-game="where-winds-meet"]` block that sets `--color-accent` to `#3fb98f` and `--color-accent-secondary` to `#d4af37`
    - `shared/theme.css` uses `auto-fill` (not `auto-fit`) in the card-grid grid-template-columns
    - `shared/theme.css` includes a `:focus-visible` rule with `outline: 2px solid var(--color-accent)`
    - `README.md` contains the `python -m http.server 8080` run command
    - `git ls-files` over the created files returns zero uppercase-containing names
  </acceptance_criteria>
  <done>.nojekyll, shared/theme.css, and README.md exist; theme tokens and per-game accent override mechanism are defined with locked hex values; no uppercase filenames introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Build the hub landing page + favicon (end-to-end deployable slice)</name>
  <files>favicon.svg, index.html</files>
  <read_first>
    - shared/theme.css (the tokens and component classes created in Task 1 — link these, do not redefine)
    - .planning/phases/01-site-foundation-hub-shell/01-UI-SPEC.md (Hub Landing layout, Game Card structure, Favicon concept, Copywriting Contract, Footer, Responsive Breakpoints)
    - .planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md (CF-02, CF-04, D-04, D-05, D-08, D-10)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pattern 1 inline registry + card renderer; Pattern 4 SVG favicon; Hub Landing Page Shell code example; Pitfall 5 single-card centering)
  </read_first>
  <action>
    Create `favicon.svg` at repo root: a 32×32 viewBox SVG GT monogram per UI-SPEC — rounded-rect background fill #0f1115 (rx 6), letter "G" in #e8e4d8 and letter "T" in #3fb98f (jade), using the system-ui font-family. This reflects hub identity (THEME-03, D-10), is game-agnostic, and is referenced root-absolute and all-lowercase.

    Create `index.html` (repo root) as the hub landing page. In <head>: charset UTF-8, viewport meta, <title>Game Tools Hub</title>, the favicon link `<link rel="icon" href="/favicon.svg" type="image/svg+xml">`, and the theme stylesheet `<link rel="stylesheet" href="/shared/theme.css">` (both root-absolute). Body has NO data-game attribute (hub is game-agnostic — D-01/UI-SPEC: hub stays neutral). Layout (D-04, tools-first): a compact `<header class="site-header">` with `<span class="site-title">Game Tools Hub</span>` (no nav links — breadcrumb is the nav pattern and the root has no ancestors), then `<main>` containing `<div id="card-grid" class="card-grid"></div>`, then `<footer class="site-footer">Game Tools Hub — personal project</footer>`.

    In an inline `<script>` define the games registry array per CF-04 with a single entry: { slug: "where-winds-meet", name: "Where Winds Meet", toolCount: 1, href: "/games/where-winds-meet/", cover: "/games/where-winds-meet/cover.svg" }. Define `renderCards(items, containerId)` that maps each item to an `<a class="card" href="${item.href}">` containing a `.card__cover` with `<img src="${item.cover}" alt="${item.name} cover art" width="280" height="160">`, a `.card__body` with `<h2 class="card__name">${item.name}</h2>` and `<p class="card__count">${item.toolCount} tool${item.toolCount !== 1 ? 's' : ''}</p>`, joins them, and assigns to the container's innerHTML. Call it on DOMContentLoaded against "card-grid" (HUB-01). The "1 tool" pluralization must be data-driven so a second registry entry (HUB-05) renders correctly with zero structural edits. Do NOT pass any URL-derived, location-derived, or user-supplied string into innerHTML — registry data is static developer-defined only (RESEARCH Security Domain; establishes the safe-innerHTML habit for Phase 2+).
  </action>
  <verify>
    <automated>cd "E:/Projekty/GithubIO" && grep -q "viewBox" favicon.svg && grep -q "3fb98f" favicon.svg && grep -q "href=\"/shared/theme.css\"" index.html && grep -q "href=\"/favicon.svg\"" index.html && grep -q "/games/where-winds-meet/" index.html && grep -q "id=\"card-grid\"" index.html && grep -q "where-winds-meet/cover.svg" index.html && ! grep -q "data-game" index.html && test -z "$(git ls-files favicon.svg index.html | grep -E '[A-Z]')" && echo PASS</automated>
    <human-check>Serve repo root (`python -m http.server 8080`), open http://localhost:8080: page background is dark (#0f1115), one centered (not lonely/left-flush) Where Winds Meet card is visible with cover panel + name + "1 tool", and the GT favicon shows in the browser tab.</human-check>
  </verify>
  <acceptance_criteria>
    - `favicon.svg` has a `viewBox` and contains the jade fill `#3fb98f`
    - `index.html` references `/shared/theme.css` and `/favicon.svg` with root-absolute paths
    - `index.html` body does NOT carry a `data-game` attribute (hub stays neutral)
    - `index.html` games registry has the WWM entry with `href: "/games/where-winds-meet/"` and `cover: "/games/where-winds-meet/cover.svg"`
    - `renderCards` pluralizes via `toolCount !== 1 ? 's' : ''` (data-driven, not hardcoded)
    - Rendered card markup wraps the whole card in a single `<a>` (full clickable target)
    - No uppercase filenames introduced
  </acceptance_criteria>
  <done>Hub landing page renders one WWM card from the inline registry, links to /shared/theme.css and /favicon.svg root-absolutely, shows the GT favicon, and the registry/renderer is drop-in extensible (one array entry adds one card).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — inbound) | No user input, no forms, no query params consumed; all page data is static developer-defined registry arrays. |
| browser → GitHub Pages CDN | Read-only static file fetch; no compute, no auth, no state on the server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (XSS) | renderCards() innerHTML in index.html | mitigate | Registry data is 100% static developer-defined literals; never pass location.href, URL params, document.cookie, or any user/URL-derived value into innerHTML. Code convention established here for Phase 2+. |
| T-01-02 | Information Disclosure | public repo contents | accept | Static personal hub; no secrets, tokens, API keys, or PII committed. Nothing sensitive to disclose. |
| T-01-03 | Tampering (supply chain) | dependencies | accept (N/A) | Zero external/npm/CDN dependencies in this phase; nothing to confuse or compromise. No package-install tasks. |
| T-01-SC | Tampering | npm/pip/cargo installs | N/A | No package-manager installs occur in this phase (per RESEARCH Package Legitimacy Audit: none). No legitimacy checkpoint required. |
| — | Spoofing / Repudiation / DoS / Elevation | — | N/A | No auth, no sessions, no privileged actions, no server compute — these categories have no surface on a static, no-input, no-backend page. |
</threat_model>

<verification>
- `.nojekyll` present at repo root and empty.
- theme.css defines base tokens + the `[data-game="where-winds-meet"]` accent override with locked jade/gold hex values.
- favicon.svg and index.html link assets root-absolutely; favicon renders in tab.
- Hub renders exactly one WWM card from the inline registry; card is the full clickable link to /games/where-winds-meet/.
- `git ls-files | grep -E '[A-Z]'` over this plan's files returns zero results.
- All asset references are root-absolute (no `../` relative paths).
</verification>

<success_criteria>
- Served repo root shows a dark-themed hub with a single, centered, non-lonely Where Winds Meet card (HUB-01, THEME-01).
- Custom GT favicon visible in the browser tab (THEME-03).
- theme.css accent mechanism proven: `[data-game="where-winds-meet"]` overrides --color-accent to jade (THEME-02 mechanism in place; applied on game pages in plan 02).
- Adding a second games-array entry renders a second card with no other file edits (HUB-05).
- Deployment guards in place from the first wave: .nojekyll committed, lowercase filenames only (SITE-02).
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-hub-shell/01-01-SUMMARY.md` when done.
</output>
