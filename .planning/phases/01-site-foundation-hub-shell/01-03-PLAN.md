---
phase: 01-site-foundation-hub-shell
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [SITE-01, SITE-02, HUB-04, HUB-05]
user_setup:
  - service: github-pages
    why: "Publish the static site at kcharkiewicz.github.io (SITE-01)"
    dashboard_config:
      - task: "Enable GitHub Pages: Source = Deploy from a branch, Branch = main, Folder = /(root). Repo must be public and named exactly kcharkiewicz.github.io (all-lowercase)."
        location: "GitHub repo Settings → Pages"
must_haves:
  truths:
    - "The site is live at https://kcharkiewicz.github.io and the hub page returns HTTP 200"
    - "All three hierarchy URLs return HTTP 200 on the live deployment (no 404, no Jekyll drops)"
    - "No tracked filename contains an uppercase character (case-safe for the Linux Pages host)"
    - "Adding a temporary second games-registry entry renders a second card with no other file edits, then reverts cleanly"
  artifacts: []
  key_links:
    - from: "git push (main)"
      to: "https://kcharkiewicz.github.io"
      via: "GitHub Pages Deploy from a branch"
      pattern: "kcharkiewicz.github.io"
---

<objective>
Take the assembled static site live and prove it works on the real GitHub Pages deployment: push the code, enable Pages (Deploy from a branch), then verify with live curl smoke tests that all three hierarchy URLs return 200, run the case-sensitivity audit, and demonstrate the drop-in registry extensibility. This is the final slice that turns the local walking skeleton into a real, bookmarkable, deployed site.

Purpose: Realizes SITE-01 (live at kcharkiewicz.github.io), closes out SITE-02 (no Jekyll drops, lowercase paths verified against the live Linux host), HUB-04 (every URL bookmarkable, 200 on the wire), and confirms HUB-05 by mutation (one registry entry → one card, no rework).
Output: A live deployment + verification evidence in the plan SUMMARY. No source files modified (the temporary extensibility-check edit is reverted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-hub-shell/01-CONTEXT.md
@.planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md
@.planning/phases/01-site-foundation-hub-shell/01-VALIDATION.md
@.planning/phases/01-site-foundation-hub-shell/01-01-SUMMARY.md
@.planning/phases/01-site-foundation-hub-shell/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Case audit, commit, and push to the kcharkiewicz.github.io repo</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (Key Decisions: .nojekyll first, all-lowercase, root-absolute paths)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pitfall 1 Jekyll, Pitfall 2 case sensitivity, Pitfall 6 Pages enablement; Environment Availability — repo state unknown)
    - .planning/phases/01-site-foundation-hub-shell/01-VALIDATION.md (Wave 0 requirements; structural audits)
  </read_first>
  <action>
    Run the case-sensitivity audit BEFORE anything ships: `git ls-files | grep -E '[A-Z]'` must return zero results (RESEARCH Pitfall 2 — Linux Pages host is case-sensitive). If any uppercase tracked filename exists, rename to lowercase via `git mv` and re-run until clean. Confirm `.nojekyll` exists at repo root and is tracked (RESEARCH Pitfall 1).

    Verify the git remote. Check `git remote -v`: the origin must point at the GitHub repo named exactly `kcharkiewicz.github.io` (all-lowercase, a user/org apex-domain site — RESEARCH Pitfall 3/6). If no remote is set, use the `gh` CLI to confirm/create a PUBLIC repo named `kcharkiewicz.github.io` (`gh repo view kcharkiewicz.github.io` to check; `gh repo create kcharkiewicz.github.io --public` if it does not exist) and add it as origin. If `gh` is not authenticated, stop and surface an auth gate (do not guess credentials).

    Stage and commit all Phase 1 site files (.nojekyll, shared/theme.css, favicon.svg, index.html, README.md, games/where-winds-meet/cover.svg, games/where-winds-meet/index.html, games/where-winds-meet/tools/spawn-timer/index.html) on the `main` branch, then push to origin/main. Do this via the SDK commit handler where available; otherwise a plain `git add` + `git commit` + `git push -u origin main`. End the commit message with the required Co-Authored-By trailer.
  </action>
  <verify>
    <automated>cd "E:/Projekty/GithubIO" && test -z "$(git ls-files | grep -E '[A-Z]')" && git ls-files --error-unmatch .nojekyll >/dev/null 2>&1 && git remote -v | grep -q "kcharkiewicz.github.io" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep -E '[A-Z]'` returns zero results (no uppercase tracked filenames)
    - `.nojekyll` is a tracked file at repo root
    - `git remote -v` shows an origin pointing at `kcharkiewicz.github.io`
    - All eight Phase 1 site files are committed on `main` and pushed to origin
  </acceptance_criteria>
  <done>All site files are case-clean, committed on main, and pushed to the public kcharkiewicz.github.io repo with .nojekyll present.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable GitHub Pages (Deploy from a branch)</name>
  <files></files>
  <what-built>Code is pushed to origin/main; .nojekyll and lowercase paths verified. The repo is ready to publish — but first-time GitHub Pages enablement is a one-time Settings action with no reliable CLI for the "Deploy from a branch" source.</what-built>
  <action>
    HUMAN ACTION (no CLI for first-time "Deploy from a branch" enablement):
    1. Open the repo on GitHub → Settings → Pages.
    2. Under "Build and deployment" → Source, select "Deploy from a branch".
    3. Branch: select `main`; Folder: select `/ (root)`. Click Save.
    4. Confirm the repository is Public (Pages on Free requires a public repo — RESEARCH Assumption A2).
    5. Wait up to ~10 minutes for the first deploy; the Pages panel shows "Your site is live at https://kcharkiewicz.github.io/".
  </action>
  <how-to-verify>
    1. Open the repo on GitHub → Settings → Pages.
    2. Under "Build and deployment" → Source, select "Deploy from a branch".
    3. Branch: select `main`; Folder: select `/ (root)`. Click Save.
    4. Confirm the repository is Public (Settings → General → Danger Zone shows "Make private" if it is currently public). Pages on Free requires a public repo (RESEARCH Assumption A2).
    5. Wait up to ~10 minutes for the first deploy. The Pages settings panel will show "Your site is live at https://kcharkiewicz.github.io/".
  </how-to-verify>
  <verify>
    <automated>MANUAL — GitHub Pages settings panel reports "Your site is live at https://kcharkiewicz.github.io/" (no CLI assertion for first-time Deploy-from-a-branch enablement; live curl is asserted in Task 3).</automated>
  </verify>
  <resume-signal>Type "approved" once the Pages panel shows the site is live (or paste the error if it does not appear after 10 minutes).</resume-signal>
  <done>GitHub Pages is enabled with Source = Deploy from a branch (main, /root) on a public repo, and the Pages panel reports the site is live.</done>
</task>

<task type="auto">
  <name>Task 3: Live smoke tests + drop-in extensibility check</name>
  <files></files>
  <read_first>
    - .planning/phases/01-site-foundation-hub-shell/01-VALIDATION.md (Per-Task Verification Map; Manual-Only Verifications; extensibility check method)
    - .planning/phases/01-site-foundation-hub-shell/01-RESEARCH.md (Pitfall 6 propagation window; SITE-01 curl smoke command)
    - index.html (the games registry array — the extensibility check edits this temporarily)
  </read_first>
  <action>
    Run live smoke tests against the deployed site (SITE-01, HUB-04, SITE-02 on the wire). curl each of the three URLs and assert HTTP 200:
      https://kcharkiewicz.github.io/
      https://kcharkiewicz.github.io/games/where-winds-meet/
      https://kcharkiewicz.github.io/games/where-winds-meet/tools/spawn-timer/
    Use `curl -s -o /dev/null -w "%{http_code}"` per URL. If any returns 404, allow for propagation (wait and retry within the ~10-minute window per RESEARCH Pitfall 6); if still 404 after the window, surface it as a deployment gap (likely Pages source mis-set or a case/path mismatch — re-check the audit) rather than silently passing.

    Run the HUB-05 drop-in extensibility check by mutation (VALIDATION Manual-Only): temporarily insert a SECOND object into the `games` array in index.html (e.g. a placeholder { slug, name, toolCount, href: "/", cover: "/games/where-winds-meet/cover.svg" }) WITHOUT editing any other file, confirm a second card renders under a local server (`python -m http.server 8080`), then REVERT the edit so index.html returns to the single-WWM-card state (and is not committed/pushed in its mutated form). Record the before/after in the SUMMARY as evidence that adding a game is a one-array-entry operation.
  </action>
  <verify>
    <automated>for u in "https://kcharkiewicz.github.io/" "https://kcharkiewicz.github.io/games/where-winds-meet/" "https://kcharkiewicz.github.io/games/where-winds-meet/tools/spawn-timer/"; do code=$(curl -s -o /dev/null -w "%{http_code}" "$u"); echo "$u -> $code"; [ "$code" = "200" ] || exit 1; done && echo PASS</automated>
    <human-check>In a browser, visit each of the three live URLs: dark theme + jade accent render, the GT favicon shows in the tab, breadcrumbs work, and no page 404s. Confirm the temporary second-card extensibility edit was reverted (index.html shows only the WWM card and git status is clean).</human-check>
  </verify>
  <acceptance_criteria>
    - `curl -s -o /dev/null -w "%{http_code}"` returns 200 for all three live URLs
    - The live hub page renders the WWM card; the stub URL returns 200 (no 404, no Jekyll drop)
    - The HUB-05 extensibility check is demonstrated (second card rendered under local server) and then reverted
    - `git status` is clean after the extensibility check (index.html back to single-card state, not committed in mutated form)
  </acceptance_criteria>
  <done>The site is live at kcharkiewicz.github.io with all three hierarchy URLs returning 200; the drop-in registry extensibility is demonstrated and reverted; the repo is in its committed single-card state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → GitHub | Authenticated `git push` / `gh` over HTTPS; standard GitHub credential handling. |
| GitHub Pages CDN → public internet | Read-only static file delivery; no compute, no state, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Information Disclosure | public repo + public Pages site | accept | Site is intentionally public; no secrets, tokens, env files, or PII are committed (verify none added before push). |
| T-01-07 | Tampering | repo write access | transfer | Repository write access is governed by GitHub account security (2FA / SSH/PAT) — outside this static site's control; relied upon as platform control. |
| T-01-08 | Spoofing | apex domain identity | accept | kcharkiewicz.github.io is GitHub-issued; HTTPS served by GitHub. No custom-domain DNS to misconfigure in this phase. |
| T-01-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH Package Legitimacy Audit: none). |
| — | Repudiation / DoS / Elevation | — | N/A | No server compute, sessions, or privileged in-app actions; DoS is GitHub's platform concern. |
</threat_model>

<verification>
- `git ls-files | grep -E '[A-Z]'` returns zero results (SITE-02 case safety on the live Linux host).
- `.nojekyll` present and tracked (SITE-02 Jekyll bypass).
- All three live URLs return HTTP 200 via curl (SITE-01, HUB-04).
- HUB-05 demonstrated by mutation and reverted; repo left in committed single-card state.
</verification>

<success_criteria>
- Site is live and reachable at https://kcharkiewicz.github.io (SITE-01).
- Every page, asset link, and internal URL works on the live deployment — no 404s, no Jekyll-dropped files, all-lowercase paths (SITE-02, HUB-04, success criterion 3).
- Adding a second game requires only one registry-array entry — proven and reverted (HUB-05, success criterion 4).
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-hub-shell/01-03-SUMMARY.md` when done, including the live curl results and the extensibility before/after evidence.
</output>
